generic_hw_control_loop:
  loop_hz: 1000
  cycle_time_error_threshold: 0.01

hardware_interface:
  joints:
    - $(arg prefix)_finger_left_joint
    - $(arg prefix)_finger_right_joint
  sim_control_mode: 0 # 0: position, 1: velocity
